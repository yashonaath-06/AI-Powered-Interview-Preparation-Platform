"""
SQLAlchemy ORM models live here.

Phase 5 will fill this package with:
    • user.py        — User
    • question.py    — Question
    • interview.py   — InterviewSession, Answer
    • resume.py      — Resume

For now we only need the package to exist so `from app import models`
works inside database.py.
"""
