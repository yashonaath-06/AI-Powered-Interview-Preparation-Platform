# ============================================================
#  Backend Python dependencies
#  Phase 2 baseline. Heavier ML deps are added in later phases
#  to keep the initial install fast for first-time users.
# ============================================================

# --- Web framework ---
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# --- Settings / validation ---
pydantic==2.6.4
pydantic-settings==2.2.1
email-validator==2.1.1

# --- Database ---
sqlalchemy==2.0.29
alembic==1.13.1
psycopg2-binary==2.9.9
# fallback driver for local sqlite dev
aiosqlite==0.20.0

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # 4.1+ removes __about__ which passlib 1.7.4 still inspects

# --- Utilities ---
httpx==0.27.0
python-dotenv==1.0.1
loguru==0.7.2
tenacity==8.2.3
pypdf>=4.2.0           # Phase 13 — resume PDF text extraction

# --- Testing ---
pytest==8.1.1
pytest-asyncio==0.23.6

# ----------------------------------------------------------------
# AI / ML libraries (added incrementally in later phases):
#   openai-whisper, transformers, sentence-transformers,
#   torch, opencv-python, mediapipe, deepface, spacy,
#   PyPDF2, language-tool-python, groq, openai
# ----------------------------------------------------------------
