# ============================================================
#  OPTIONAL "heavy" ML dependencies.
#
#  These power the real Whisper (Phase 8), real NLP scorer
#  (Phase 9), and real computer-vision (Phase 10) features.
#
#  The platform RUNS WITHOUT these — every AI service
#  detects them at import time and falls back gracefully.
#
#  Install with:
#      pip install -r requirements.txt -r requirements-ml.txt
#  Or, with Docker, the ML profile already includes them.
# ============================================================

# ---- Phase 8: Whisper speech-to-text ----
faster-whisper>=1.0.3
numpy<2.0
ctranslate2>=4.0.0,<5.0.0

# ---- Phase 9: NLP scorer ----
sentence-transformers>=2.7.0
textstat>=0.7.3
# torch is pulled in transitively by sentence-transformers; on Linux/macOS the
# default wheel is the CPU build (~200 MB). For Windows/CUDA see PyTorch docs.

# ---- Phase 10: Computer vision ----
mediapipe==0.10.14
opencv-python>=4.10.0,<5.0.0
Pillow>=10.0
# NOTE: mediapipe 0.10.14 ships its own opencv-python dependency. On Linux
# you may need 'libGL.so.1' system-wide:
#     dnf install mesa-libGL          # Amazon Linux / Fedora / RHEL
#     apt-get install -y libgl1       # Debian / Ubuntu
